# Job Scheduler Service - Python Dependencies

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Database ORM
sqlalchemy==2.0.23
aiosqlite==0.19.0
alembic>=1.13.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.2

# Utilities
python-dotenv==1.0.0
PyYAML==6.0.1
httpx==0.25.1

# Rate limiting
slowapi==0.1.9

# v2.0 Lab module
# ZH: docker SDK 用於 lab_manager 啟動 / 停止 code-server 容器
# EN: docker SDK used by lab_manager to start/stop code-server containers
docker==7.1.0

# ZH: cryptography 用於 secrets_service AES-256-GCM 加解密
# EN: cryptography used by secrets_service for AES-256-GCM encryption
cryptography>=42.0.0

# ZH: openpyxl 用於 admin 使用者管理 Excel 匯出 (v2.2 新增)
# EN: openpyxl for admin user-management Excel export (v2.2)
openpyxl==3.1.5

# ZH: python-pptx 用於 v2.3 P1「文書簡報」agent 生成 .pptx
#     Pillow 為 python-pptx 處理圖片的相依（預留封面/圖示用）
# EN: python-pptx for v2.3 P1 presentation agent (.pptx generation)
#     Pillow is python-pptx's image backend (reserved for cover/icon use)
python-pptx==1.0.2
Pillow>=10.4.0