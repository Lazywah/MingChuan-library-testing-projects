<!-- apm:rules:start -->
<!-- 由 `apm rules --claude-md` 產生。規則改了就重跑一次，不要手改。 -->

## 程式碼慣例（由 apm 監測）

這個專案用 apm 監測實作進度。它**不看宣稱，只看證據** ——
所以下面幾條不是風格偏好，是會被實際檢查的東西。

判準是**「會不會騙到人」**：明講自己沒做的不算問題，
假裝做完了的才是。

### 1. 刻意留空的實作要寫 docstring，不是行尾註解

空函式如果是刻意的，用 docstring 說明原因。
行尾註解不算數 —— 工具只認 docstring。

```python
def on_disconnect(self):
    """連線中斷時的處理。目前不需要做任何事，保留給未來擴充。"""
```

<sub>對應規則：`bare_pass`、`explained_pass`（未校準）</sub>

### 2. 還沒做就明講，不要回傳假的成功值

`raise NotImplementedError("原因")` **不算坑** —— 它大聲說自己沒做，不騙人。
但「有 TODO 卻 `return True`」**是坑** —— 它假裝驗過了。

```python
# 可以：誠實的待辦
def send_email(to, subject):
    raise NotImplementedError("等金流商確認 API 規格")

# 不行：宣稱通過但根本沒驗
def validate(payload):
    # TODO: 補上欄位檢查
    return True
```

<sub>對應規則：`not_implemented`、`lying_todo`</sub>

### 3. 未完成標記要用慣例形式

`TODO:` 開頭，或標記後面接冒號／括號／破折號。
散文裡出現 bug、todo 這些字不算標記，工具不會誤抓。

<sub>對應規則：`marker`</sub>

### 4. 寫了函式就接上，不然先不要寫

沒有任何呼叫者的函式會被列入上線前審核清單。
**特別注意呼叫端的函式名有沒有打錯** —— 打錯的話在靜態分析上
跟死碼長得一模一樣，而且會炸在沒人看的路徑上。

<sub>對應規則：`never_called`（未校準）、`caller_typo`（未校準）</sub>

### 5. 測試要有真的斷言

`assertTrue(True)`、`assertEqual(x, x)` 這種恆真斷言等於沒寫。
`with self.assertRaises(...)` 算斷言，不用擔心。

<sub>對應規則：`vacuous_assertion`、`no_assertion`（未校準）</sub>

### 6. 用到的套件要寫進依賴宣告

只裝在本機、沒寫進 `requirements.txt` / `package.json` 的套件，
換一台機器就爆。
**不確定套件名的時候先確認它存在**，不要憑印象寫。

<sub>對應規則：`phantom_dependency`（未校準）、`hallucinated_import`（未校準）</sub>

### 7. subagent 要有穩定的角色名字

apm 會把每一隻 subagent 畫成一隻小寵物，**坑帳分開記** ——
所以你看得出哪一隻挖了坑。

身分（綽號、顏色、坑帳）綁在**角色**上，也就是 agent 定義檔的名字，
不是 agent id。每次派任務都是一個新的 id，綁在 id 上的話同一個角色跑三次
就長出三隻不同顏色的寵物，其中兩隻永遠不會再動。

所以：**給 subagent 一個穩定的定義檔名字**（`.claude/agents/<名字>.md`），
不要每次臨時取。同角色同時跑兩隻是「分身」—— 帳記在一起。

⚠️ 沒有收到 `SubagentStop` 的 subagent，畫面上顯示**「安靜」**而不是「睡覺」。
那兩個是不同的事：前者是「不知道牠結束了沒」，後者是「確定收工了」。
hook 掛掉的時候你會看到前者。

### 8. 宣稱完成之前先跑一次 apm scan

「完成」的四項證據：檔案存在、有實作、有測試、**有人真的呼叫它**。
任一項不成立而你宣稱完成，就會被標成「疑似造假」。

```
apm scan
```


### 這些規則的來歷

「坑」的定義來自人工標記（2026-08-24 的 16 題 + 08-26 的 5 題），不是憑感覺定的。

標「未校準」的意思是**沒有掛門檻、沒有加權** —— 不是「完全不影響判定」。
⚠️ 這裡原本寫的是後者，而那是假的：`vacuous_assertion` 與 `skipped_test`
從第一天就會讓宣稱完成的功能降級，兩條卻都標著未校準。
2026-08-26 補上人工標記並改掉這句話。

完整的規則台帳（含每條的涵蓋範圍與校準狀態）：跑 `apm rules`。

⚠️ 這一段是**貼進你自己專案的 CLAUDE.md** 的。上面那行原本給的是
apm 原始碼裡的一個檔案路徑 —— 那個檔案不在你的專案裡，照著去找會找不到。
現在給的是一個真的走得通的指令。

⚠️ 程式碼範例一律寫成 Python。判定規則本身對 JS/TS 也適用
（樣式類與標記類偵測不分語言），但範例沒有翻譯 ——
JS 專案的讀者請自行對應。

<!-- apm:rules:end -->
