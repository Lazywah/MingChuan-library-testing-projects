# 04 - 系統管理與維護 | System Administration & Maintenance

## 📑 目錄 | Table of Contents
- [Admin UI 視覺化管理 (推薦) \| Visual Admin Dashboard](#admin-ui-視覺化管理-推薦--visual-admin-dashboard)
- [資料庫備份 \| Database Backup](#資料庫備份--database-backup)
- [日誌查看 \| Log Viewing](#日誌查看--log-viewing)
- [系統監控 \| System Monitoring](#系統監控--system-monitoring)
- [資料庫查閱 (手動指令) \| Database Inspection](#資料庫查閱-手動指令--database-inspection)
- [Token 管理 (手動指令) \| Token Management](#token-管理-手動指令--token-management)
- [儲存空間 \| Storage](#儲存空間--storage)

---

## Admin UI 視覺化管理 (推薦) | Visual Admin Dashboard

本系統已整合獨立的**管理員面板 (Admin UI)**，大幅降低需要透過 SSH 或終端機指令進行維護的需求。
請使用瀏覽器存取 `http://<服務層IP>:8888/` 並以管理員身分登入。

### 1. 系統設定檔管理 (System Config Management)
您不再需要透過 SSH 修改設定檔。在 Admin UI 的 **「Config Management」** 頁籤中，您可以直接線上讀取與寫入以下核心設定檔：
- `.env`: 環境變數與郵件伺服器設定。
- `docker-compose.yml`: 核心服務架構。
- `docker-compose.ai-models.yml`: 模型服務架構。
- `scheduler_policy.yaml`: GPU 排程與結點池設定。
- `sso_policy.yaml`: SSO 驗證設定。
*(註：修改 `.env` 等底層環境變數後，仍需透過 SSH 重啟容器 `docker compose up -d` 才會生效。)*

### 2. 帳號與 Token 管理 (User & Token Management)
在 **「User Management」** 頁籤中，您可以：
- **快速建立帳號 (Provision)**：無須使用者自行註冊，管理員可直接配發帳號，系統會透過 SMTP 發送歡迎信與密碼。
- **重設密碼 (Reset)**：為忘記密碼的使用者一鍵重設，並寄送新密碼信件。
- **批量配發額度 (Batch Tokens)**：選取多名使用者，一鍵將他們的 Token 使用量歸零，或調整其配額上限。

---
## 資料庫備份 | Database Backup

### 手動備份
```bash
# ZH: 複製 SQLite 資料庫檔案 | EN: Copy SQLite database file
cp data/ai_platform.db backups/ai_platform_$(date +%Y%m%d_%H%M%S).db
```

### 排程備份 (Linux crontab)
```bash
# 每天凌晨 3 點備份
0 3 * * * cp /opt/ai-platform/data/ai_platform.db /opt/ai-platform/backups/daily_$(date +\%Y\%m\%d).db
```

### 資料庫完全重置 (Destructive Reset)
> [!CAUTION]
> 此操作將永久刪除所有使用者、任務與對話紀錄。執行前請務必確認已完成資料備份。

當系統資料異常或需要重新開始時使用：
1. **停止服務**：`docker compose stop job-scheduler`
2. **刪除檔案**：`rm data/ai_platform.db*`
3. **重啟服務**：`docker compose up -d job-scheduler`
（系統啟動時會自動根據 `models.py` 重新建立空白資料表）

## 日誌查看 | Log Viewing

```bash
# 所有服務日誌
docker compose logs

# 特定服務 (即時追蹤)
docker compose logs -f job-scheduler
docker compose logs -f open-webui

# 最近 100 行
docker compose logs --tail=100 job-scheduler
```

## 系統監控 | System Monitoring

```bash
# 容器狀態
docker compose ps

# 資源使用量
docker stats

# 健康檢查
curl http://localhost:8002/health
```

## 資料庫查閱 (手動指令) | Database Inspection

### 推薦工具
建議下載 `data/ai_platform.db` 後使用 **DB Browser for SQLite**。

### 常用查詢指令 (透過 Docker)
```bash
# 查看所有帳號
docker exec ai-platform-scheduler python -c "import sqlite3; conn = sqlite3.connect('/data/ai_platform.db'); [print(row) for row in conn.execute('SELECT username, role FROM users').fetchall()]"

# 查看任務佇列情況
docker exec ai-platform-scheduler python -c "import sqlite3; conn = sqlite3.connect('/data/ai_platform.db'); [print(row) for row in conn.execute('SELECT job_id, status, progress FROM training_jobs ORDER BY created_at DESC LIMIT 5').fetchall()]"
```

## Token 管理 (手動指令) | Token Management

> **推薦：** 建議直接在 Admin UI 的 User Management 頁面中使用「Batch Action」進行 Token 的重置與派發。以下為進階或自動化腳本使用的手動指令。

Token 會在每月第 `TOKEN_RESET_DAY` 天自動重置。

### 手動將所有使用者的 Token 消耗歸零 (Reset all users' tokens to 0)
```bash
docker exec ai-platform-scheduler python -c "import sqlite3; conn = sqlite3.connect('/data/ai_platform.db'); conn.execute('UPDATE token_usage SET tokens_used = 0'); conn.commit()"
```

### 手動調整用戶額度
```bash
docker exec ai-platform-scheduler python -c "import sqlite3; conn = sqlite3.connect('/data/ai_platform.db'); conn.execute('UPDATE token_usage SET tokens_limit=10000000 WHERE user_id=\"USER_ID\"'); conn.commit()"
```

## 儲存空間 | Storage

| 目錄 | 用途 | 預估大小 |
|------|------|----------|
| `data/` | SQLite 資料庫 | < 100MB |
| Docker volumes | Open WebUI + Ollama 模型 | 5-50GB |

### 清理 Docker 暫存
```bash
docker system prune -f
```
