# 10 - 使用者操作手冊 | User Manual

## 📑 目錄 | Table of Contents
- [角色權限說明 \| Role Overview](#-角色權限說明--role-overview)
- [快速上手 \| Getting Started](#-快速上手--getting-started)
- [運算任務操作 \| Compute Tasks Guide](#-運算任務操作--compute-tasks-guide)（含 Notebook / High / Mid-Low 頁籤）
- [AI 助手操作 \| AI Assistant (Hub)](#-ai-助手操作--ai-assistant-hub)
- [系統設定 \| Settings](#️-系統設定--settings)
- [教師功能 \| Teacher Features](#-教師功能--teacher-features)
- [管理員功能 \| Admin Panel](#️-管理員功能--admin-panel)
- [常見問題 \| FAQ](#-常見問題--faq)

---
歡迎使用 AI 訓練助理平台！本手冊旨在幫助研究人員與學生快速熟悉系統功能與操作介面。

---

## 👥 角色權限說明 | Role Overview

系統共有三種角色，帳號由管理員配發：

| 角色 | 說明 | 訓練任務 | AI 助手 | Admin Panel |
|------|------|:---:|:---:|:---:|
| **student（學生）** | 一般使用者，僅能查看自己的任務 | 僅自己 | ✅ | ❌ |
| **teacher（教師）** | 可監控全系統任務，適合課堂管理 | 所有人 | ✅ | ❌ |
| **admin（管理員）** | 完整系統控制，含帳號與模型管理 | 所有人 | ✅ | ✅ |

> [!NOTE]
> Token 月度配額由管理員統一設定，各角色皆受配額限制。教師帳號的配額預設與學生相同，管理員可依需求個別調整。

---

## 🚀 快速上手 | Getting Started

在進入系統後，您會先看到 **教學導覽**，之後直接進入 **運算任務 (Compute Tasks)** 頁面。

### 1. 登入系統
- 請使用提供的帳號與密碼進行登入。
- SSO 模擬使用者可直接以學號同時作為帳號與密碼登入 (如 `T1090001` / `T1090001`)。
- 系統採用的 **JWT 安全授權** 具備自動到期機制，若登入失效請重新刷新頁面。

### 2. 入站教學
- 首次登入時，系統會自動顯示 **「Welcome to AI Base!」** 的教學面板。
- 導覽介紹三大板塊：**運算任務**、**AI 助手**、**系統設定**。
- 勾選「不再顯示」可永久關閉；前往「設定 → 教學手冊 → 開啟教學」可重新開啟。

---

## ⚡ 運算任務操作 | Compute Tasks Guide

運算任務頁面是您提交與監控訓練任務的核心位置（原「儀表板」已更名為此）。頁面上方有三個子頁籤：

---

### 📓 Notebook 頁籤（主要使用方式）

類 Google Colab 的互動式程式編輯環境，讓您直接在瀏覽器內撰寫與執行 AI 訓練程式。

#### 工具列設定

| 選項 | 說明 |
|------|------|
| **框架 (Framework)** | 選擇訓練框架：PyTorch、TensorFlow、HuggingFace Transformers、llama.cpp、vLLM 或自訂 Image |
| **模式 (Mode)** | 選擇任務類型：訓練 (Training)、微調 (Fine-tuning)、推論 (Inference)、資料前處理 (Preprocessing) |
| **GPU 節點** | 選擇執行節點（「Auto」表示由系統自動安排）|
| **▶ Run All** | 將所有格子合併執行，提交至 GPU 排程佇列 |
| **■ Stop** | 中止當前執行中的任務 |

#### 程式格子 (Cells)

Notebook 支援三種格子類型：

| 格子類型 | 說明 | 範例 |
|----------|------|------|
| **Code（程式格）** | Python 程式碼，支援語法高亮 | `import torch; print(torch.cuda.is_available())` |
| **Shell（指令格）** | 任意 Shell 指令（bash、g++、nvcc 等） | `pip install deepspeed` 或 `nvidia-smi` |
| **Markdown（說明格）** | 純說明文字，不執行 | `# 這是我的訓練腳本` |

#### 操作說明

1. **新增格子**：點擊底部的「+ Code」、「+ Shell」或「+ Markdown」按鈕。
2. **上下移動格子**：點擊格子右側的 ▲ / ▼ 按鈕調整順序。
3. **刪除格子**：點擊格子右側的 🗑 按鈕。
4. **選擇框架模板**：切換框架 / 模式後，系統會詢問是否套用對應的起始範本程式碼。
5. **資料集上傳**：在工具列下方的資料集列點擊「上傳」，選取 `.csv`、`.jsonl` 或 `.zip` 檔案。
6. **自動儲存**：格子內容每次修改後 2 秒自動儲存，頁面頂端會顯示「已儲存 ✓」。

#### 執行流程

```
撰寫格子 → Run All → 系統合併所有格子 → 提交 Job → GPU Worker 領取 → 容器執行
                                                                     ↓
                                                          輸出面板即時顯示日誌與進度條
```

> [!NOTE]
> **「Run All」機制說明**：點擊 Run All 時，所有 Code 格子與 Shell 格子會依序合併為一支腳本提交執行（與 Colab 的「Run All Cells」相同）。格子之間的 Python 變數可完整共享。

#### 輸出面板

執行後，頁面下方的輸出面板會即時顯示：
- **執行日誌**（容器 stdout）：訓練輸出、錯誤訊息等
- **進度條**：解析自 `Epoch N/M`、`step N/M`、`Progress: X%` 等常見格式

---

### 左右分欄頁籤（備用快速表單）

除了 Notebook 之外，還提供兩個簡易提交頁籤，適合快速派發標準任務：

#### High Compute 頁籤
- 適用 Priority ≥ 2 的高優先急件任務

#### Mid / Low Compute 頁籤  
- 適用 Priority 0 ~ 1 的正常與批次任務

兩個頁籤均提供：任務名稱、模型識別碼、優先級、Epochs / Batch Size 等欄位，點擊 **「Dispatch Task」** 即可派發。任務提交後，可在任務卡片上看到即時進度條。

---

## 🤖 AI 助手操作 | AI Assistant (Hub)

全新的 AI 大廳採用分類導覽機制。

### 1. 導覽大廳 (AI Hub)
- **AI 模型**：進行文字聊天、圖片分析與知識搜尋。
- **文書寫作**：輔助撰寫論文摘要、合約分析或會議簡報。
- **影音創作**：生成示意圖、腳本與背景音樂。

### 2. 對話功能
- **串流回傳**：AI 會隨想隨讀，實現即時反應。
- **多對話管理**：點擊左側「開始新對話」可建立獨立的討論串；點擊歷史紀錄可切換回舊對話。
- **帳號隔離**：每位使用者的聊天紀錄獨立儲存，不同帳號登入只會看到自己的對話。
- **返回大廳**：若需切換分類功能，請點擊對話視窗上方的「🔙 返回大廳」按鈕。

---

## ⚙️ 系統設定 | Settings

設定頁面包含以下功能區塊：

| 區塊 | 說明 |
|------|------|
| **Token Resources** | 代幣配額圓環進度條，顯示已使用量、總配額與重置日期。 |
| **Profile Update** | 個人資料變更，支援隨時修改電子信箱與密碼。採跨層驗證機制。 |
| **Appearance** | 深色/淺色主題切換。 |
| **Localization** | 中英文語言即時切換。 |
| **Tutorial Guide** | 「開啟教學」按鈕，可重新顯示入站教學面板。 |
| **Account** | 登出系統按鈕。 |

> [!NOTE]
> **佈局說明**：設定頁面已全面優化為「單欄式橫向條列佈局 (Responsive Horizontal Bars)」，確保在任何螢幕尺寸下都能精確操作各項選項。

---

## 📋 教師功能 | Teacher Features

以 **teacher** 角色登入時，介面與學生相同，但以下功能有所不同：

### 全域任務監控
- 在「運算任務 (Compute Tasks)」頁面，教師可看到**所有使用者**提交的訓練任務，而非僅限自己的任務。
- 任務卡片會額外顯示提交者帳號，方便課堂掌握學生訓練進度。
- 教師仍**無法取消**他人的任務（該操作僅限管理員）。

### 其他功能
- AI 助手、系統設定、Token 配額查詢等功能與學生完全相同。
- 若需進行帳號管理或系統設定，請聯絡管理員。

---

## 🛡️ 管理員功能 | Admin Panel

當以 **admin** 角色帳號登入時，導覽列會多出一個 **「管理中心 (Admin)」** 入口（網址：`/admin-ui/`）。

### 使用者管理
- 查看所有帳號的信箱、角色、在線狀態、最後登入 IP / 時間及 Token 用量。
- **建立帳號**：點擊「Provision User」配發新帳號，系統自動產生臨時密碼並寄信通知。
- **編輯帳號**：點擊列表中的使用者可修改角色、啟用狀態、Token 上限與密碼（需輸入管理員密碼解鎖）。
- **批量設定 Token 上限**：輸入新額度後點擊「Apply to All」套用至全體使用者。
- **批量歸零用量**：點擊「Reset All Usage」將所有使用者的本月已用量歸零。
- **個別歸零用量**：在使用者編輯視窗點擊「Reset Usage」，僅歸零該使用者的用量。

### 模型管理
- 新增、編輯、刪除 AI 模型。支援 **API 模型**（雲端：Anthropic / OpenAI / Google / Ollama）與**本地模型**兩種類型。

### 全域排程
- 查看全系統的訓練任務，可篩選狀態（pending / running / completed 等）。
- **強制取消**任務或**調整優先級**（0–5，數字越大越先執行）。

### 系統設定檔
- 直接在管理介面編輯 `.env`、`scheduler_policy.yaml`、`sso_policy.yaml` 等設定檔，儲存後即時生效。

### 數據分析
- 查看各學系 Token 用量分布圖與工具使用佔比，支援依學系篩選。

---

## ❓ 常見問題 | FAQ

### Q: 如果 AI 回到一半出現紅色提示？
這是由於網路波動或配額不足造成的中斷。系統會**自動保留**已生成的文字內容，您可以嘗試重新發送訊息或到設定頁面檢查 Token 餘額。

### Q: 如何切換中英文介面？
請前往「系統設定 (Settings)」分頁，點擊「語言切換」按鈕即可。大廳與對話介面將會即時同步變更。

### Q: 代幣配額在哪裡查看？
代幣配額已移至「系統設定 (Settings)」分頁的第一個區塊中。
